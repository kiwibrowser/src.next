# Build APKs for Kiwi Browser Next
name: 'Kiwi: Build test apk'

# Controls when the action will run. Triggers the workflow on push or pull request events
on:
  workflow_dispatch:

concurrency: build_test_apk

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    steps:
      - name: Get branch name
        id: branch-name
        uses: tj-actions/branch-names@v4.8
    
      - name: Building APK
        run: curl --no-buffer -s 'https://${{ secrets.BUILD_SERVER }}/build_apk.php?build_key=${{ secrets.BUILD_KEY }}&git_user=${{ github.repository_owner }}&git_repository=${{ github.event.repository.name }}&git_runid=${{ github.run_id }}&git_branch=${{ steps.branch-name.outputs.current_branch }}'

      - name: Downloading APK
        run: wget 'https://${{ secrets.BUILD_SERVER }}/apks/${{ github.run_id }}/out.apk'

      - name: Creating release
        id: create_release
        uses: actions/create-release@v1.1.4
        with:
          tag_name: ${{ github.run_id }}
          release_name: Test build ${{ github.run_id }} initiated by ${{ github.actor }}
          draft: true
          prerelease: true
          body: |
            This is a test build initiated by ${{ github.actor }} to test the build system. Do not install.

      - name: Uploading test apk (arm64)
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./out.apk
          asset_name: ${{ github.repository_owner }}.${{ github.event.repository.name }}.${{ steps.branch-name.outputs.current_branch }}.${{ github.run_id }}-unofficial-${{ github.repository_owner }}-arm64.apk
          asset_content_type: application/vnd.android.package-archive
