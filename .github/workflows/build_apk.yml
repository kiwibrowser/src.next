# Build APKs for Kiwi Browser Next
name: 'Any branch: Build APK'

# Controls when the action will run
on:
  pull_request:
    branches:
      - kiwi
  workflow_dispatch:

concurrency: build_test_apk

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Maximize build space
        run: |
          echo "Before cleanup:"
          df -h
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo docker image prune --all --force
          echo "After cleanup:"
          df -h

      - name: Checkout Kiwi code
        uses: actions/checkout@v4
        with:
          ref: kiwi
          path: kiwi-patches
          fetch-depth: 0
          
      - name: Load Chromium version
        run: |
          if [ -f "src/CHROMIUM_VERSION" ]; then
            source src/CHROMIUM_VERSION
            while IFS= read -r var; do
              export "$var"
            done < <(cut -d= -f1 src/CHROMIUM_VERSION | grep -vF '#')
            env | grep -F CHROMIUM_ >> "$GITHUB_ENV"
            echo "Building Chromium ${CHROMIUM_MAJOR}.${CHROMIUM_MINOR}.${CHROMIUM_BUILD}.${CHROMIUM_PATCH}"
          fi

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            python3 python3-pip git curl wget \
            build-essential ninja-build pkg-config \
            libnss3-dev libglib2.0-dev libgtk-3-dev \
            libatk1.0-dev libatk-bridge2.0-dev \
            libcups2-dev libdrm-dev libxkbcommon-dev \
            libgbm-dev libasound2-dev

      - name: Setup depot_tools
        run: |
          git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
          echo "${GITHUB_WORKSPACE}/depot_tools" >> "$GITHUB_PATH"

      - name: Configure gclient for Chromium
        run: |
          export PATH="${GITHUB_WORKSPACE}/depot_tools:$PATH"
          # Determine Chromium version from kiwi-patches
          if [ -f "kiwi-patches/CHROMIUM_VERSION" ]; then
            source kiwi-patches/CHROMIUM_VERSION
            CHROMIUM_TAG="${CHROMIUM_MAJOR}.${CHROMIUM_MINOR}.${CHROMIUM_BUILD}.${CHROMIUM_PATCH}"
            echo "Fetching Chromium version: ${CHROMIUM_TAG}"
          else
            echo "Warning: CHROMIUM_VERSION not found, using latest"
            CHROMIUM_TAG="105.0.5195.24"
          fi
          
          # Create .gclient file for Chromium Android build
          cat > .gclient << EOF
          solutions = [
            {
              "name": "src",
              "url": "https://chromium.googlesource.com/chromium/src.git@${CHROMIUM_TAG}",
              "managed": True,
              "custom_deps": {},
              "custom_vars": {
                "checkout_android": True,
              },
            },
          ]
          target_os = ["android"]
          EOF

      - name: Sync Chromium source
        run: |
          export PATH="${GITHUB_WORKSPACE}/depot_tools:$PATH"
          # Sync with --nohooks to avoid optional tool download failures
          gclient sync --no-history --shallow --nohooks
          
      - name: Run essential build hooks
        run: |
          export PATH="${GITHUB_WORKSPACE}/depot_tools:$PATH"
          # Run hooks but allow non-critical failures
          cd src
          python3 build/landmines.py || true
          python3 build/util/lastchange.py -o build/util/LASTCHANGE || true
          python3 build/linux/sysroot_scripts/install-sysroot.py --arch=arm64 || true
          python3 tools/clang/scripts/update.py || true

      - name: Apply Kiwi modifications
        run: |
          # Copy Kiwi-specific modifications over Chromium base
          rsync -avz --progress --exclude=".git" kiwi-patches/ src/

      - name: Configure build
        run: |
          export PATH="${GITHUB_WORKSPACE}/depot_tools:$PATH"
          cd src
          gn gen out/Default --args='
            target_os="android"
            target_cpu="arm64"
            is_debug=false
            is_official_build=false
            is_component_build=false
            symbol_level=0
            enable_nacl=false
            remove_webcore_debug_symbols=true
          '

      - name: Build APK
        run: |
          export PATH="${GITHUB_WORKSPACE}/depot_tools:$PATH"
          cd src
          autoninja -C out/Default chrome_public_apk

      - name: Prepare artifacts
        run: |
          mkdir -p artifacts
          cp src/out/Default/apks/*.apk artifacts/ || true
          ls -lah artifacts/

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: kiwi-browser-apk-${{ github.run_id }}
          path: artifacts/*.apk
          retention-days: 30
          if-no-files-found: error

      - name: Get branch name
        id: branch-name
        uses: tj-actions/branch-names@v4.8

      - name: Create release
        if: github.event_name != 'pull_request'
        id: create_release
        uses: actions/create-release@v1.1.4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: build-${{ github.run_id }}
          release_name: Build ${{ github.run_id }} by ${{ github.actor }}
          draft: true
          prerelease: true
          body: |
            Automated build initiated by ${{ github.actor }}
            Branch: ${{ steps.branch-name.outputs.current_branch }}
            Commit: ${{ github.sha }}
            Built on GitHub Actions

      - name: Upload release asset
        if: github.event_name != 'pull_request'
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: artifacts/ChromePublic.apk
          asset_name: KiwiBrowser-${{ steps.branch-name.outputs.current_branch }}-${{ github.run_id }}-arm64.apk
          asset_content_type: application/vnd.android.package-archive

      - name: Build summary
        run: |
          {
            echo "### Build Summary :rocket:"
            echo "- **Chromium Version:** ${CHROMIUM_MAJOR}.${CHROMIUM_MINOR}.${CHROMIUM_BUILD}.${CHROMIUM_PATCH}"
            echo "- **Branch:** ${{ steps.branch-name.outputs.current_branch }}"
            echo "- **Commit:** ${{ github.sha }}"
            echo "- **Run ID:** ${{ github.run_id }}"
            echo "- **Artifacts:** Available in workflow artifacts"
          } >> "$GITHUB_STEP_SUMMARY"

